### v2.0.8

**🐛 修复指令检测失效**

* 修复 `/` 开头指令消息未被正确跳过的问题：指令检测改为读取原始消息文本，避免被平台适配器剥离。

---

### v2.0.7

**🛡️ 稳定性优化修复**

* 修复合并消息放回缓冲区后无主动触发机制导致消息永远卡住的问题：现在自动调度延迟重试，不再依赖用户发送新消息。
* 新增连续失败计数器：直接 LLM 调用连续失败超过 3 次后放弃消息，避免无限重试循环。
* 修复会话清理条件不完整可能提前销毁会话的问题。
* 新增主动僵尸会话清理定时器：每 30 秒扫描一次，不再依赖用户发消息才触发被动检测。
* 修复对话历史过长可能导致 token 超限无限重试的问题：现在限制最近 20 条对话历史。
* 修复 `command_prefixes` 配置为字符串时指令识别异常的问题。

---

### v2.0.6

**🐛 修复对话历史保存失败 & 图片构造方式错误**

* 修复图片消息构造方式与框架 API 不对齐导致图片丢失的问题。
* 修复对话历史中图片信息未保存的问题。

---

### v2.0.5

**🐛 修复消息丢失致命 bug & 增强失败恢复**

* 修复直接 LLM 调用失败时销毁整个会话导致所有合并消息丢失的致命 bug：现在将消息放回缓冲区等待恢复。
* 直接 LLM 调用失败时不再重试主模型，避免越权绕过框架 fallback、加剧限流风险。
* 新增失败恢复机制：将合并消息安全放回缓冲区，等待新消息触发时走框架管道。

---

### v2.0.4

**🐛 修复多个严重 bug**

* 修复僵尸会话清理后消息丢失：清理后缺少返回导致消息被吞掉。
* 修复对无 session 用户的 LLM 回复被静默吞掉：发送指令的用户、群聊未启用的用户的回复不再被错误拦截。
* 修复中断标志未正确重置可能导致用户收到重复回复或后续正常响应被误判为中断的问题。

---

### v2.0.3

**🐛 修复图片/表情包丢失问题**

* 修复中断时图片无法回填到缓冲区的问题。
* 修复对话历史不保存图片信息的问题。
* 修复纯图片消息（无文本）时上下文构建异常的问题。

---

### v2.0.2

**🐛 修复对话历史读写失败**

* 修复对话历史读取和保存均失败的问题：属性访问方式与框架不对齐。
* 现已使用安全访问方式，兼容旧版 AstrBot。

---

### v2.0.1

**🐛 修复中断机制多个严重 bug**

* 修复中断标志被清除导致被中断的 LLM 响应永远无法拦截的致命 bug。
* 修复中断重试时原始消息丢失的问题：新增防抖结算保存机制，中断发生时回填到缓冲区头部，确保重试请求包含完整上下文。
* 修复会话清理未正确设置事件导致协程永久挂起（死锁）的严重 bug。
* 修复达到最大重试次数后旧 LLM 响应泄露给用户的 bug。
* 中断时清空响应文本，防止 PostSplitter 部分发送被中断的响应。

---

### v2.0.0

**🔄 架构重设计：中断立即重试，不再依赖 LLM 响应触发**

* 中断时立即启动防抖+重试，不再等待 LLM 响应来触发重试。
* LLM 响应钩子仅负责丢弃被中断的响应，不再承担重试职责。
* 新增回复发送后清理会话的钩子，防止僵尸会话。

**🛡️ 关键修复**

* 新增代际计数器：每次中断递增，完成后检查代际是否匹配。若已被新中断取代，自动丢弃过期响应，避免用户收到多条重复回复。
* 修复达到最大重试次数的判断逻辑错误。
* 修复完成后残余缓冲区消息直接丢失的问题：现在会启动新的防抖处理残留消息。
* 移除 `session_timeout` 用户配置项，改为内部 60 秒僵尸会话检测作为最后防线。

---

### v1.4.0

**🐛 修复僵尸会话导致不回复的严重 bug**

* 新增会话超时机制：会话处于 LLM 处理状态超过 120 秒后强制清理。
* 根因：LLM 响应钩子在某些情况下不会触发（如事件被其他插件终止），导致会话永远卡住，后续所有消息都被当作"中断"处理并被吞掉，形成死锁。
* 修复会话超时后被清理后，新消息无法正确创建新会话的问题。

---

### v1.3.0

**🐛 修复防抖结算后 session 过早清理导致消息无法合并的严重 bug**

* 防抖结算后不再清理 session，而是保留并标记 LLM 处理中状态。
* 修复前：三条连续消息被分成三次独立防抖结算，完全没有合并。
* 修复后：第一条消息防抖结算后 session 保留，后续消息能正确进入中断重试流程。

---

### v1.2.0

**🐛 修复最大中断次数逻辑 & 增强健壮性**

* 修复达到最大中断次数后仍调用 `stop_event()` 导致新消息被吞的 bug：现在达到上限后放行新消息由框架正常处理。
* 修复达到最大中断次数后缓冲区消息永远不会被处理的 bug。
* 新增后台任务跟踪机制，插件卸载时正确取消所有后台任务。
* 新增配置值验证：`debounce_time` 和 `max_retry_count` 不允许为负数。

---

### v1.1.1

**🐛 修复中断重试相关 bug**

* 修复中断重试时图片丢失的问题。
* 修复达到最大中断次数后缓冲区消息丢失的问题。
* 修复对话历史无法保存的问题。

---

### v1.1.0

**🔄 架构重构：兼容框架插件生态**

* 防抖阶段不再自行调用 LLM，改为重构事件后交由框架处理，其他插件（dayflow、记忆召回、postsplitter 等）正常生效。
* 中断重试改为在 LLM 响应钩子中检测中断标记，丢弃被中断的响应后异步重新请求。
* 修复对话历史无法保存的问题。
* 移除了自行管理对话历史的逻辑，框架会自动处理。

---

### v1.0.1

**🐛 修复指令放行逻辑**

* 修复收到指令时错误地发起 LLM 请求的 bug。
* 现在收到指令时会正确取消所有防抖和 LLM 任务，清空会话缓冲区，然后放行给框架处理。

---

### v1.0.0

**🔀 首次发布：用户消息智能合并与中断重试插件上线**

**1. ⏳ 防抖收集**

* 收到用户消息后等待可配置的防抖时间再发起 LLM 请求。
* 防抖期间新消息追加到缓冲区并重置计时器。
* 防抖时间设为 0 可跳过等待，立即发起请求。

**2. ⚡ 中断重试**

* LLM 请求进行中，用户发送新消息时自动中断当前请求。
* 被中断的请求全程无痕：不发送提示、不记录对话历史、不触发其他插件钩子。
* 合并缓冲区消息后重新发起 LLM 请求。

**3. 🔀 消息合并**

* 多条消息按可配置分隔符（默认换行符）合并为一条。
* 自动识别图片并与文本一起传递给 LLM。

**4. 📋 指令放行**

* 以配置前缀开头的消息识别为指令，不参与合并，直接放行。
* 收到指令时立即结束当前防抖会话并刷新缓冲区。

**5. 👥 按用户隔离**

* 每个用户独立的消息缓冲区和 LLM 请求，互不干扰。
* 支持私聊和群聊（群聊默认关闭，可配置开启）。

**6. 🛡️ 安全限制**

* 最大中断重试次数（默认 5 次），避免无限中断循环。
* 达到上限后等待当前请求完成，新消息缓存待后续处理。
